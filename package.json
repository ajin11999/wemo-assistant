{
  "name": "wemo-assistant",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "deploy:backend": "npm run --prefix apps/backend deploy",
    "deploy:admin": "npm run --prefix apps/admin deploy",
    "deploy:all": "npm run deploy:backend && npm run deploy:admin",
    "db:migrate:remote": "npm run --prefix apps/backend db:migrate:remote",
    "dev:backend": "bun run --cwd apps/backend dev",
    "dev:admin": "bun run --cwd apps/admin dev"
  }
}
